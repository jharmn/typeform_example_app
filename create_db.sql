create database restart_example;
use restart_example;
create table Entries (ID MEDIUMINT NOT NULL AUTO_INCREMENT, Token varchar(255) NOT NULL, FirstName varchar(255) NOT NULL, LastName varchar(255), Email varchar(255) NOT NULL, ImageUrl varchar(255), PRIMARY KEY (id));
